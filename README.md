# 📌 Automated UI Tests for effective-mobile.ru
**Python 3.10 + Playwright + Pytest + Allure + Docker**

Этот проект содержит автоматизированные UI-тесты главной страницы сайта **effective-mobile.ru**, выполненные с использованием Playwright и паттерна Page Object.
Проект легко запускается локально или в Docker-контейнере.

## 🚀 Возможности проекта

### ✔ UI тесты
- Проверка переходов по всем основным секциям главной страницы:
  - «О нас»
  - «Контакты»
  - «Услуги»
  - и другие блоки
- Валидация корректного URL после клика.
- Проверка наличия ключевых элементов.
- Использование паттерна **Page Object**.
- Логи и отчёты Allure.

## 📁 Структура проекта

```
effective-mobile-tests/
│
├── pages/
│   ├── base_page.py
│   └── home_page.py
│
├── tests/
│   └── test_home_page.py
│
├── conftest.py
├── requirements.txt
├── Dockerfile
├── README.md
└── pytest.ini
```

## 🧩 Установка и запуск локально

### 1️⃣ Установить зависимости
```bash
pip install -r requirements.txt
```

### 2️⃣ Установить браузеры Playwright
```bash
playwright install
```

### 3️⃣ Запуск тестов
```bash
pytest
```

### 4️⃣ Отчёт Allure
```bash
pytest --alluredir=allure-results
allure serve allure-results
```

## 🐳 Запуск в Docker

### 1️⃣ Собрать образ
```bash
docker build -t effective-tests .
```

### 2️⃣ Запустить контейнер
```bash
docker run --rm effective-tests
```

## 👨‍💻 Обновление локаторов
Актуализируйте селекторы в:
```
pages/home_page.py
```

